These files contain the monthly climate variables with the strongest correlations. Each file contains information for a single species at all three sites. Variables names with a 1 after the variable are for trees at SPRY (the NW side). Variables names with a 2 after the variable are for trees at SUNR (the E side). Variables names with nothing after the variable are for trees at PARA (the S side).